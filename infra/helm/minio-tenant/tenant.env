# Tenant configuration
NAMESPACE="minio-tenant"
RELEASE_NAME="lakehouse-minio"
VALUES_FILE="values.yaml"

# Optional override for pool name (fallback to values.yaml if unset)
# POOL_NAME="pool-0"

# Port-forward artifacts
CONSOLE_LOG_FILE="console-port-forward.log"
CONSOLE_PID_FILE="console-port-forward.pid"
API_LOG_FILE="api-port-forward.log"
API_PID_FILE="api-port-forward.pid"
